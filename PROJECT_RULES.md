# PROJECT_RULES

> 本文件是项目的长期约束。任何开发者或 AI 在修改项目之前，都必须先阅读本文件。与本文件冲突的修改不得直接执行，必须先建立变更请求。

## 1. 项目目标

本项目是个人股票研究和趋势位置看板，用于集中展示自选股的关键位置和核心指标，帮助进行快速横向比较。

本项目不是：

- 高频交易系统
- 自动交易系统
- 券商终端
- 实时报价系统
- 投资建议生成器
- 财务预测系统

本项目长期优先级是：

- 数据准确
- 指标口径一致
- 移动端可读
- 能快速判断股票相对强弱
- 历史需求可追踪
- AI 修改行为可控制

## 2. 核心数据定义

### 年线

- 长期默认定义：`MA250`
- 含义：约 250 个交易日的移动平均价格
- 距离年线百分比表达：`当前价格 / MA250 - 1`
- 项目当前以百分比数值形式存储和展示，而不是 `0~1` 小数

必须保持：

- 正值表示股价位于年线上方
- 负值表示股价位于年线下方
- 未经正式变更记录，不得将 `MA250` 改为 `MA200`、`MA240` 或其他周期

### YTD

- `YTD` 表示年初至今涨跌幅
- 当前代码确认使用当年首个有效交易日的收盘价作为基准，而不是自然日 `1 月 1 日`
- 当前代码确认使用 `close` 作为计算基准
- 同一复权模式下，当前价格与基准价格必须使用同一口径

表达方式：

- `当前价格 / 年内首个有效交易日收盘价 - 1`

### 52 周高点

- 52 周高点必须使用对应周期内的最高成交价字段 `high`
- 不得使用最高收盘价 `close`
- 当前代码确认使用最近 `252` 个交易日窗口

表达方式：

- `当前价格 / 最近 252 个交易日最高 high - 1`

必须保持：

- 该值通常小于或等于 `0`
- 未经正式变更记录，不得将口径改为 `close`

### 前复权和不复权

项目长期需要支持：

- `qfq`
- `none`

必须保持：

- 同一指标中的当前价格与历史基准必须使用同一复权口径
- 不允许混合计算
- 数据口径切换后，所有关联指标必须同步切换
- 前端不得自行重算与 Python 不一致的指标结果

### 流通市值

- 流通市值是项目背景中曾提及的潜在关注数据
- 但当前仓库代码、JSON 和前端中未确认存在该字段
- 在未建立正式需求和实现前，不得将其写成“当前已实现事实”

## 3. 数据处理原则

必须遵守：

1. 关键指标优先在 Python 数据层统一计算。
2. React 前端主要负责展示、排序和交互。
3. 不应在多个前端组件中重复实现相同指标公式。
4. 当前百分比字段统一以“百分比数值”存储，例如 `5.14` 表示 `5.14%`。
5. 所有时间字段必须明确时区或来源。
6. 页面必须显示数据时间信息。
7. 页面最好显示数据生成时间；如果同时有交易日与生成时间，两者应可区分。
8. 缺失数据不得伪装成 `0`。
9. 计算失败时应保留错误或缺失状态。
10. 不得用静默填充值掩盖数据问题。

当前已确认事实：

- `updated_at` 由北京时间 `Asia/Shanghai` 生成
- `errors` 字段仅在有错误时写入 JSON
- 单只股票异常不会自动导致整批数据生成失败

## 4. UI 和交互原则

当前设计方向与长期要求：

- 浅色模式
- 简洁、信息优先的看板风格
- 移动端优先
- 不追求传统券商终端式高密度堆叠
- 重点数字应便于快速扫描
- 桌面端和移动端均应可用
- 表格应支持横向滚动
- 股票名称和代码层级清晰
- 排名使用较弱视觉层级
- 当前排序列可以有清晰但克制的高亮
- 不使用过度动画
- 不为视觉效果牺牲数据可读性

基于当前代码可确认的交互事实：

- 页面有顶部汇总卡片
- 页面支持指标切换
- 页面支持复权口径切换
- 表格区域为横向滚动布局
- 排名列和股票信息列当前使用 sticky 方式固定

如果未来需要改变固定列行为、主表结构或移动端信息层次，必须通过需求变更流程确认。

## 5. 工程原则

必须遵守：

- TypeScript 不随意使用 `any`
- 公共数据类型应集中定义或保持统一
- 不重复实现相同指标逻辑
- 不进行与任务无关的大规模重构
- 不在一次修改中混合多个无关目标
- 不随意更换技术栈
- 不随意增加依赖
- 新依赖必须有必要性说明
- 读取现有实现后再修改
- 保持现有命名与目录风格
- 修改后必须运行相应检查

## 6. AI Coding 行为规则

AI 在修改本项目之前，必须先阅读：

1. `PROJECT_RULES.md`
2. `docs/REQUIREMENTS.md`
3. `docs/DECISIONS.md`
4. `docs/CHANGE_REQUESTS.md`

AI 必须遵守：

1. 明确本次处理的需求编号。
2. 如果用户新指令与已确认需求冲突，不得静默覆盖旧需求。
3. 发生冲突时，必须指出冲突，并建议建立 Change Request。
4. 不得因为“优化”“美化”“重构”等模糊指令擅自改变：
   - 指标口径
   - 数据字段
   - 业务逻辑
   - 页面核心结构
   - 自动部署机制
5. 不得删除看似无用但尚未确认无用的逻辑。
6. 必须避免修改无关文件。
7. 完成后必须报告：
   - 修改文件
   - 修改原因
   - 对应需求编号
   - 验收标准
   - 测试结果
   - 未解决风险

可直接复制给 AI 的任务模板：

```text
请先阅读：

1. PROJECT_RULES.md
2. docs/REQUIREMENTS.md
3. docs/DECISIONS.md
4. docs/CHANGE_REQUESTS.md

本次任务只处理需求：<需求编号>

任务目标：
<具体目标>

允许修改：
<文件或目录>

禁止修改：
<文件、数据口径或功能>

验收标准：
1. ...
2. ...
3. ...

如果本次指令与已有需求冲突，请先指出冲突，不要直接覆盖原需求。

完成后请输出：
1. 修改文件列表
2. 每个文件的修改原因
3. 对应需求编号
4. 验收结果
5. 测试结果
6. 数据口径是否变化
7. 尚未解决的问题
```

## 7. Definition of Done

一个需求只有同时满足以下条件，才能标记为完成：

- 需求有唯一编号
- 需求内容明确
- 验收标准明确
- 代码已经实现
- 静态检查通过
- 构建通过
- 相关功能经过验证
- 移动端影响已检查
- 数据口径没有被意外改变
- 文档已更新
- `docs/CHANGELOG.md` 已更新
- 没有无关改动

## 8. 需求从讨论进入正式管理的流程

正式流程：

```text
产生想法
-> 与 AI 讨论
-> 明确需求边界
-> 分配需求编号
-> 写入 REQUIREMENTS.md
-> 必要时建立 ADR
-> 编码实现
-> 对照验收标准测试
-> 状态改为 Implemented
-> 更新 CHANGELOG
-> Git commit 引用需求编号
```

需求变更流程：

```text
发现原需求需要改变
-> 建立 Change Request
-> 记录原需求和新需求
-> 分析数据、前端、部署和兼容性影响
-> 接受、拒绝或延期
-> 原需求标记为 Superseded
-> 建立或更新新需求
-> 实现和测试
-> 更新 CHANGELOG
```

## 9. Git 提交规范

建议提交格式：

```text
feat(UI-002): improve mobile stock column layout
fix(DATA-003): use high price for 52-week high
docs(ADR-002): document 52-week high definition
refactor(DATA-004): centralize adjustment mode handling
chore(CR-004): record mobile table behavior change
```

允许的常用类型：

- `feat`
- `fix`
- `docs`
- `refactor`
- `test`
- `chore`
- `perf`

避免无意义提交信息：

- `update`
- `fix`
- `final`
- `changes`
- `stuff`
- `new version`
